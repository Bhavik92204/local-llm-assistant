# Target for Next.js rewrites (server-side). Browser calls /jarvis-api/* instead.
# Defaults to 127.0.0.1:8000 if unset.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Optional explicit backend URL for next.config.mjs (overrides NEXT_PUBLIC_API_URL)
# BACKEND_INTERNAL_URL=http://127.0.0.1:8000
